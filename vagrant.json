{
    "variables": {
        "password": "rancher",
        "version": "v0.10.0-12-gb0710ed-amd64",
        "checksum": "fc7276330ee704f995d03f82624491283b0c8b0a5b37b6101d8a3b82dee41025",
        "iso_url": "https://github.com/cwebd/k3os/releases/download/{{user `version`}}/k3os-amd64.iso"
    },
    "builders": [
        {
            "type": "virtualbox-iso",
            "boot_wait": "6s",
            "boot_command": [
                "<enter>",
                "<wait20>",
                "rancher",
                "<enter>",
                "sudo k3os install",
                "<enter>",
                "1",
                "<enter>",
                "y",
                "<enter>",
                "http://{{ .HTTPIP }}:{{ .HTTPPort }}/cloud-config.yml",
                "<enter>",
                "y",
                "<enter>"
            ],
            "guest_os_type": "Linux_64",
            "http_directory": ".",
            "iso_url": "{{ user `iso_url` }}",
            "iso_checksum": "{{user `checksum`}}",
            "iso_checksum_type": "sha256",
            "ssh_username": "rancher",
            "ssh_password": "{{user `password`}}",
            "ssh_wait_timeout": "1000s",
            "disk_size": "8000",
            "shutdown_command": "sudo poweroff",
            "post_shutdown_delay": "10s",
            "format": "ova"
        }
    ],
    "provisioners": [
        {
            "type": "shell",
            "execute_command": "{{ .Vars }} sudo -E sh '{{ .Path }}'",
            "inline": [
                "rm -rf /var/lib/rancher/k3s"
            ]
        }
    ],
    "post-processors": [
        {
            "type": "vagrant",
            "keep_input_artifact": false,
            "output": "k3os_{{.Provider}}.box",
            "vagrantfile_template": "vagrantfile.template"
        }
    ]
}
